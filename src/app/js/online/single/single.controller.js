(function() {
    'use strict';

    angular.module('readerApp.online.single.controller', [
        'readerApp.online.single.service',
        'readerApp.config'
    ]).controller('ComicSingleController', ComicSingleController);

    ComicSingleController.$inject = [];
    function ComicSingleController() {

        // view model

        // events

        init();

        /**
         * @return void
         */
        function init() {

        }
    }
})();
